# Frontend

Avskalad Expo-router-app med tre sidor:

- `app/index.tsx` for inloggning
- `app/home.tsx` for home
- `app/profile.tsx` for profil

Starta lokalt med:

```bash
npm install
npm run start
```
